% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_learning_track.R
\name{get_learning_tracks}
\alias{get_learning_tracks}
\title{Title
#' Get learning tracks (optionally filtered by skill)}
\usage{
get_learning_tracks(con, skill_id = NULL)

get_learning_tracks(con, skill_id = NULL)
}
\arguments{
\item{con}{Database connection (DBI)}

\item{skill_id}{}
}
\value{
Data frame with columns: \code{track_id}, \code{title}, \code{description}, \code{url}
}
\description{
Returns learning tracks from \code{adem_learning_tracks}. If \code{skill_id} is provided,
filters to only tracks associated with that specific skill.
}
\examples{
\dontrun{
con <- connect_db()

# All learning tracks
get_learning_tracks(con)

# Tracks for specific skill
get_learning_tracks(con, skill_id = "skill_r")

dbDisconnect(con)
}

}
